[Unit]
Description=OCR Cascade Demo — delete uploaded documents
After=network.target

[Service]
Type=oneshot
User=ocr
ExecStart=/bin/bash -c 'rm -rf /opt/ocr-cascade/runs/* && echo "Demo cleanup: runs/ cleared at $(date)"'
